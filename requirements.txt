# Recommended Python: 3.11

python-dotenv
numpy

# Matched PyTorch stack
torch==2.10.0
torchaudio==2.10.0
torchcodec==0.10.0

# Speaker diarization / verification
pyannote.audio>=4,<5

# RAG
sentence-transformers
faiss-cpu

# Groq Whisper + LLM
groq

# UI
streamlit>=1.40.0

# FFmpeg fallback bundled through Python package
imageio-ffmpeg==0.6.0
